apiVersion: v1
kind: Service
metadata:
  name: mana-archive
  namespace: mana-archive
spec:
  selector:
    app: mana-archive
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
