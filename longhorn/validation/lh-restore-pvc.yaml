apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lh-restore-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
  volumeName: lh-restore-pv
